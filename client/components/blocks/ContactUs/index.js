import delve from 'dlv';
import Link from 'next/link';

const ContactUs = ({ title, text, buttons, theme }) => {
 return (
  <div className={`bg-${theme}`}>
   <h1>contact usss</h1>
  </div>
  );
};

ContactUs.defaultProps = {};

export default ContactUs;
